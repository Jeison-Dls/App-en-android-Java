package com.example.hospitalturnmanagement.data.model

// Clase de datos para representar la respuesta al crear un paciente
data class CreatePatientResponse(
    val id: Int // El ID del paciente recién creado (proporcionado por el backend)
)